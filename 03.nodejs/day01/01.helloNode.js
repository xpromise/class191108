/*
  运行nodejs：
    node 文件名

    1. 一定要来到当前要运行的文件所在目录
    2. 文件按tab可以自动补全

    browser中js的组成：
      DOM --> document 用来操作文档
      BOM --> window 用来操作浏览器（控制浏览记录、读取浏览器的信息...）
      ES  --> 语法规范

    nodejs中js的组成：
      DOM(完全没有)
      BOM(干掉了绝大部分，只保留少部分)
        console.log
        setInterval
        setTimeout
      ES(基本实现)

*/
console.log("hello nodejs");
